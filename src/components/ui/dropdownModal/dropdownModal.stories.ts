/*
import type { Meta, StoryObj } from '@storybook/react'

import { DropdownModal } from './'

const meta = {
  argTypes: {
    variant: {
      control: { type: 'radio' },
      options: ['headerButtom', 'headerLogo'],
    },
  },
  component: DropdownModal,
  tags: ['autodocs'],
  title: 'Components/UI/NewModal',
} satisfies Meta<typeof DropdownModal>

export default meta
type Story = StoryObj<typeof meta>

export const HeaderButtom: Story = {
  args: {
    children: 'Primary Input',
    variant: 'headerButtom',
  },
}

export const HeaderLogo: Story = {
  args: {
    children: 'Secondary Input',
    variant: 'headerLogo',
  },
}
*/
export default {}
